# 3.0 实施验证记录

日期：2026-09-17。范围为当前工作树；未切换 `D:\AIWORK\qmt-bridge` 的现有部署，
未登录或重启 QMT，未进行真实下单、撤单或历史下载。

## 自动化检查

- 全量测试：**350 passed，2 subtests passed，10.44 秒**。
- Ruff：通过；BasedPyright：0 errors / 0 warnings。
- 内嵌 Python 3.6：42 个文件语法检查通过，同时排除 `__future__.annotations`。
  这不是券商内置 Python 的真机运行验证。
- `uv lock --check`：通过。
- 构建成功：`dist/release-3.0.0-029f3986/` 下的 wheel 与 sdist。
- 两种产物分别在全新隔离环境安装：版本 3.0.0、客户端无需 MiniQMT、43 个部署源文件校验通过。
- 安装器对临时伪 QMT 根目录执行 dry-run，前后文件完全一致；包内没有 `.pyc` 或旧兼容模块。
- Windows 控制器在随机回环端口、临时日志/状态目录、无账户、写入关闭且 runtime 故意无效的
  子进程上完成启动、身份检查、优雅停止；状态与停止请求文件已移除。

测试使用隔离 `--basetemp`；本机原有 pytest 临时目录链接存在权限问题，不清理用户的该目录。

## 离线性能对照

复现命令（项目 server 环境）：

```powershell
.venv\Scripts\python.exe scripts\benchmark-bridge.py --days 252 --samples 12
```

合成数据：252 日、60,732 根分钟线、3,886,856 字节；结果逐值一致。
参考实现是同一解析器全量读取后取末尾 3 根，**不是旧部署的端到端基准**。

| 指标 | 全量解析后取尾部 | 有界尾部读取 |
|---|---:|---:|
| p50 | 285.500 ms | 2.604 ms |
| p95 | 502.390 ms | 2.849 ms |
| Python 分配峰值 | 51,436,537 bytes | 146,838 bytes |

10 个相同代码集的 mock 行情客户端：每个采样周期上游调用由独立采样的 10 次减为 1 次。
上述数字只验证本机合成数据路径，不代表 QMT 服务器吞吐、网络延迟或券商环境 SLA。

## 未完成的真机验证边界

只读现场检查时，13543 HTTP 端口及 58600 FormulaServer 端口存在监听，默认 ZMQ
15560 / 15561 没有监听。现有部署的 health / readiness GET 在各 2 秒预算内超时。
因此没有取得可用的真机原生行情返回，也没有将旧服务状态当成新版本已验证。

实际部署后仍需用只读样本验收券商版本的原生调用、订阅释放及返回字段；
真实交易和下载验收不在本次执行范围。迁移和删除清单见 [MIGRATION_3.md](MIGRATION_3.md)。
